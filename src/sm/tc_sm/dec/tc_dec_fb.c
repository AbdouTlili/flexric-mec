#include "tc_dec_fb.h"

#include <assert.h>

tc_event_trigger_t tc_dec_event_trigger_fb(size_t len, uint8_t const ev_tr[len])
{
  assert(0!=0 && "Not implemented");
  assert(ev_tr != NULL);
  tc_event_trigger_t avoid_warning;
  return avoid_warning;
}

tc_action_def_t tc_dec_action_def_fb(size_t len, uint8_t const action_def[len])
{
  assert(0!=0 && "Not implemented");
  assert(action_def != NULL);
  tc_action_def_t avoid_warning;
  return avoid_warning;
}

tc_ind_hdr_t tc_dec_ind_hdr_fb(size_t len, uint8_t const ind_hdr[len])
{
  assert(0!=0 && "Not implemented");
  assert(ind_hdr != NULL);
  tc_ind_hdr_t avoid_warning;
  return avoid_warning;
}

tc_ind_msg_t tc_dec_ind_msg_fb(size_t len, uint8_t const ind_msg[len])
{
  assert(0!=0 && "Not implemented");
  assert(ind_msg != NULL);
  tc_ind_msg_t avoid_warning;
  return avoid_warning;
}

tc_call_proc_id_t tc_dec_call_proc_id_fb(size_t len, uint8_t const call_proc_id[len])
{
  assert(0!=0 && "Not implemented");
  assert(call_proc_id != NULL);
  tc_call_proc_id_t avoid_warning;
  return avoid_warning;
}

tc_ctrl_hdr_t tc_dec_ctrl_hdr_fb(size_t len, uint8_t const ctrl_hdr[len])
{
  assert(0!=0 && "Not implemented");
  assert(ctrl_hdr != NULL);
  tc_ctrl_hdr_t avoid_warning;
  return avoid_warning;
}

tc_ctrl_msg_t tc_dec_ctrl_msg_fb(size_t len, uint8_t const ctrl_msg[len])
{
  assert(0!=0 && "Not implemented");
  assert(ctrl_msg != NULL);
  tc_ctrl_msg_t  avoid_warning;
  return avoid_warning;
}


tc_ctrl_out_t tc_dec_ctrl_out_fb(size_t len, uint8_t const ctrl_out[len]) 
{
  assert(0!=0 && "Not implemented");
  assert(ctrl_out!= NULL);
 tc_ctrl_out_t  avoid_warning;
  return avoid_warning;
}

tc_func_def_t tc_dec_func_def_fb(size_t len, uint8_t const func_def[len])
{
  assert(0!=0 && "Not implemented");
  assert(func_def != NULL);
 tc_func_def_t  avoid_warning;
  return avoid_warning;
}

