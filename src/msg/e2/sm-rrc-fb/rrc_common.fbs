namespace rrc.common;

enum RAT: uint8 { LTE, NR }
table Snssai {
  sst: uint8;
  sd: uint32;
}
table Plmn {
  mcc: uint16;
  mnc: uint16;
  mnc_length: uint8;
  snssai: [Snssai];
}
table PlmnInfo {
  plmn: Plmn;
  tac: uint32;
  ranac: uint8;
  cellId: uint64;
}
